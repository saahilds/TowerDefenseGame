package com.main.game.entity;

import javafx.scene.paint.ImagePattern;
import javafx.scene.shape.Rectangle;

public interface EntityWithBackground {
    void setEntityImgPattern(ImagePattern pattern);
}
